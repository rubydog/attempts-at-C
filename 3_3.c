#include <stdio.h>
#define MAXCHAR 1000

void expand(char s1[], char s2[]);

main() {
  char s1[MAXCHAR] = "a-z";
  char s2[MAXCHAR];

  expand(s1, s2);
}

void expand(char s1[], char s2[]) {
  
  for (int i = s1
  // 
}
